#prende lista di blocchi, controlla campo "score"
var score;
var lim=4.6;
toRem=[];
blocchi=Document.
foreach(blocco in blocchi);

score=4.5;
if(score<lim)
	toRem.add(blocco);


Blocchi.remove(toRem);